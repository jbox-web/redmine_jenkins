<%# This is used to display basic git setup instructions, like on github... %>
<% flash.now[:warning] = l(:error_no_settings, :url => url_for(:controller => 'projects', :action => 'settings', :tab => 'jenkins', :id => @project)) %>

<h2><%= l(:label_jobs_list) %></h2>

<% content_for :sidebar do %>
  <%= render :partial => 'jenkins_sidebar' %>
<% end %>
